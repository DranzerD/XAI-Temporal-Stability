# XDrift: Temporal Stability of XAI in Credit Risk Models
# Pinned versions for reproducibility — last verified May 2026

# Core scientific stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
matplotlib==3.9.2
seaborn==0.13.2

# Tree-based ML and XAI
xgboost==2.0.3
shap==0.44.1
lime==0.2.0.1
optuna==3.6.1

# HMM for regime detection
hmmlearn==0.3.2

# Statistical post-hoc tests (Phase 2)
scikit-posthocs==0.9.1

# Progress bars
tqdm>=4.66.0

# Parquet I/O (train.parquet / test.parquet saves) — not listed originally,
# to_parquet() has no fallback and will crash without one of these.
pyarrow>=14.0.0

# Notebook execution (optional, for CI)
# jupyter>=1.0.0
# nbconvert>=7.0.0
